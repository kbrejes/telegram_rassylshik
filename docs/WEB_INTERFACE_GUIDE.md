# Руководство по веб-интерфейсу Job Notification Bot

## 📋 Оглавление

- [Введение](#введение)
- [Установка и запуск](#установка-и-запуск)
- [Использование веб-интерфейса](#использование-веб-интерфейса)
- [Создание канала](#создание-канала)
- [Фильтрация сообщений](#фильтрация-сообщений)
- [API](#api)

## Введение

Веб-интерфейс позволяет управлять несколькими каналами уведомлений, настраивать источники мониторинга и фильтры для каждого канала.

### Основные возможности

✅ Создание и управление множественными каналами уведомлений  
✅ Настройка источников мониторинга для каждого канала  
✅ Гибкая система фильтрации (включающие и исключающие ключевые слова)  
✅ Включение/отключение каналов без удаления  
✅ Статистика по каналам  
✅ Простой и интуитивный интерфейс

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка .env

Убедитесь, что файл `.env` содержит необходимые параметры:

```env
API_ID=your_api_id
API_HASH=your_api_hash
PHONE=your_phone_number
```

### 3. Запуск системы

```bash
# Даем права на выполнение
chmod +x start_multi.sh

# Запускаем
./start_multi.sh
```

Или напрямую через Python:

```bash
python3 main_multi.py
```

### 4. Доступ к веб-интерфейсу

Откройте в браузере: **http://localhost:8080**

## Использование веб-интерфейса

### Главная страница

На главной странице отображается:

- Список всех каналов уведомлений
- Статус каждого канала (активен/отключен)
- Количество источников и фильтров
- Кнопки для редактирования и удаления

### Статистика

В нижней части страницы отображается:

- Общее количество каналов
- Количество активных каналов  
- Количество уникальных источников для мониторинга

## Создание канала

### Шаг 1: Нажмите "Добавить канал"

### Шаг 2: Заполните форму

#### Название канала *
Удобное название для идентификации канала.

**Пример:** `Python разработчики`, `Django вакансии`, `Remote jobs`

#### ID Telegram канала/чата *
ID канала куда будут отправляться уведомления.

**Как получить ID:**
1. Добавьте бота [@getmyid_bot](https://t.me/getmyid_bot) в канал/чат
2. Скопируйте ID (например: `-1001234567890`)

**Важно:** Бот должен иметь доступ к этому каналу/чату!

#### Источники для мониторинга *
Список каналов для отслеживания сообщений (по одному на строку).

**Формат:**
- Username: `@channel_name`
- ID: `-1001234567890`

**Пример:**
```
@python_jobs
@django_jobs  
@remote_work_ru
-1001234567890
```

#### Включающие ключевые слова *
Сообщение должно содержать **хотя бы одно** из этих слов.

**Пример для Python вакансий:**
```
python
django
flask
fastapi
backend
```

#### Исключающие ключевые слова
Сообщения с этими словами будут **отфильтрованы**.

**Пример:**
```
junior
стажер
trainee
intern
```

#### Канал активен
Снимите галочку, если хотите временно приостановить отправку уведомлений в этот канал.

### Шаг 3: Сохраните

Нажмите **"Создать канал"**. Конфигурация будет сохранена и сразу начнет работать.

## Фильтрация сообщений

### Как работает фильтрация

1. **Проверка источника:** Сообщение должно прийти из одного из настроенных источников
2. **Включающие фильтры:** Сообщение должно содержать **хотя бы одно** ключевое слово из списка
3. **Исключающие фильтры:** Сообщение **НЕ должно** содержать ни одного слова из списка

### Примеры конфигураций

#### Пример 1: Python-разработчики (Senior/Middle)

**Включающие:**
```
python
django
flask
fastapi
backend
middle
senior
```

**Исключающие:**
```
junior
стажер
trainee
```

#### Пример 2: Remote JavaScript вакансии

**Включающие:**
```
javascript
react
vue
angular
node.js
удаленно
remote
```

**Исключающие:**
```
офис
office
релокация
```

#### Пример 3: Таргетологи FB/IG

**Включающие:**
```
facebook
instagram
таргет
таргетолог
fb ads
meta ads
```

**Исключающие:**
```
google
yandex
tiktok
```

## API

Веб-интерфейс предоставляет REST API для программного управления.

### Получить список каналов

```http
GET /api/channels
```

**Ответ:**
```json
{
  "success": true,
  "channels": [
    {
      "id": "channel_abc123",
      "name": "Python разработчики",
      "telegram_id": -1001234567890,
      "enabled": true,
      "input_sources": ["@python_jobs", "@django_jobs"],
      "filters": {
        "include_keywords": ["python", "django"],
        "exclude_keywords": ["junior"],
        "require_all_includes": false
      }
    }
  ]
}
```

### Получить канал по ID

```http
GET /api/channels/{channel_id}
```

### Создать канал

```http
POST /api/channels
Content-Type: application/json

{
  "name": "Python разработчики",
  "telegram_id": -1001234567890,
  "input_sources": ["@python_jobs"],
  "include_keywords": ["python", "django"],
  "exclude_keywords": ["junior"],
  "enabled": true
}
```

### Обновить канал

```http
PUT /api/channels/{channel_id}
Content-Type: application/json

{
  "name": "Новое название",
  "enabled": false
}
```

### Удалить канал

```http
DELETE /api/channels/{channel_id}
```

### Получить статистику

```http
GET /api/stats
```

**Ответ:**
```json
{
  "success": true,
  "stats": {
    "total_channels": 3,
    "enabled_channels": 2,
    "total_input_sources": 5,
    "channels_breakdown": [
      {
        "name": "Python разработчики",
        "sources_count": 2,
        "enabled": true
      }
    ]
  }
}
```

## Архитектура системы

### Компоненты

```
┌─────────────────┐
│  Веб-интерфейс  │  FastAPI (http://localhost:8080)
│   (web/app.py)  │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ ConfigManager   │  Управление конфигурацией
│ (config_manager.py)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ MultiChannelBot │  Telegram userbot
│ (bot_multi.py)  │
└────────┬────────┘
         │
         ↓
    ┌─────────┴─────────┐
    │                   │
┌───▼────┐      ┌──────▼──────┐
│ Input  │      │   Output    │
│Sources │      │  Channels   │
└────────┘      └─────────────┘
```

### Процесс работы

1. **Веб-интерфейс:** Пользователь создает/редактирует каналы через браузер
2. **ConfigManager:** Сохраняет конфигурацию в `configs/channels_config.json`
3. **MultiChannelBot:** Загружает конфигурацию и начинает мониторинг
4. **Фильтрация:** Каждое сообщение проверяется на соответствие фильтрам каждого канала
5. **Отправка:** Подходящие сообщения отправляются во все matching output каналы

## Устранение неполадок

### Бот не получает сообщения

1. Убедитесь что Telegram userbot подключен к источникам
2. Проверьте что ID источников указаны правильно
3. Убедитесь что источники публичные или вы в них состоите

### Уведомления не приходят

1. Проверьте что канал активен (enabled = true)
2. Убедитесь что фильтры настроены правильно
3. Проверьте что бот имеет доступ к output каналу
4. Посмотрите логи: `logs/bot_multi.log`

### Веб-интерфейс не открывается

1. Проверьте что порт 8080 свободен: `lsof -i :8080`
2. Убедитесь что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи запуска

## Рекомендации

### Начальная настройка

1. Начните с 1-2 каналов и небольшого количества источников
2. Наблюдайте за результатами в течение дня
3. Постепенно добавляйте новые источники и каналы
4. Уточняйте фильтры на основе полученных результатов

### Оптимизация фильтров

- Используйте **широкие** включающие фильтры для максимального охвата
- Используйте **точные** исключающие фильтры для отсеивания нерелевантных сообщений
- Регулярно анализируйте пропущенные сообщения
- Не используйте слишком много фильтров сразу

### Безопасность

- Не делайте веб-интерфейс доступным из интернета без аутентификации
- Используйте файрвол для ограничения доступа к порту 8080
- Регулярно делайте бэкапы `configs/channels_config.json`

## Поддержка

Если возникли вопросы или проблемы, проверьте:

1. Логи: `logs/bot_multi.log`
2. Конфигурацию: `configs/channels_config.json`
3. Документацию: `README.md`, `PROJECT_SUMMARY.md`

---

**Версия:** 2.0  
**Дата:** Ноябрь 2024

