# Быстрый старт (5 минут)

## Шаг 1: Получите API ключи

1. Откройте https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение и скопируйте:
   - **API ID** (число вроде 12345678)
   - **API Hash** (строка вроде abcdef...)

## Шаг 2: Узнайте свой Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram
2. Скопируйте ваш **ID** (число вроде 123456789)

## Шаг 3: Настройте бота

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env
nano .env  # или любой другой редактор
```

Заполните эти строки в `.env`:

```env
API_ID=ваш_api_id
API_HASH=ваш_api_hash
PHONE=+ваш_номер_телефона
NOTIFICATION_USER_ID=ваш_telegram_id
```

## Шаг 4: Добавьте чаты для мониторинга

Отредактируйте `channels.txt`:

```txt
@pythonrujobs
@remotejobsrussia
@freelance_ru
```

> **Совет**: Найдите чаты с вакансиями в вашей сфере в Telegram и добавьте их username (с @) или ID

## Шаг 5: Запустите!

```bash
# Простой способ (автоматический скрипт)
./start.sh

# Или вручную с Docker
docker-compose run --rm bot python main.py  # Первый раз - введите код из SMS
docker-compose up -d                        # Запуск в фоне
```

> **Примечание:** Это упрощенная версия БЕЗ AI. Фильтрация работает по ключевым словам.  
> Подробнее: `SIMPLE_VERSION.md`

## Готово! 🎉

Теперь бот работает и будет отправлять вам уведомления о релевантных вакансиях.

### Просмотр логов

```bash
docker-compose logs -f bot
```

### Остановка

```bash
docker-compose down
```

## Что дальше?

- Настройте шаблоны ответов в `templates.json` под себя
- Добавьте больше чатов в `channels.txt`
- Прочитайте полный [README.md](README.md) для деталей

## Проблемы?

**Бот не получает сообщения:**
- Убедитесь, что вы состоите в этих чатах
- Проверьте, что username/ID правильные

**Бот не находит релевантные вакансии:**
- Отредактируйте ключевые слова в `message_processor.py`
- Смотрите логи: что именно фильтруется

**Нужна помощь:**
Откройте issue на GitHub или проверьте секцию "Устранение проблем" в README.md

