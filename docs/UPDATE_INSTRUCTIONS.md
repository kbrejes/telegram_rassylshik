# 🔄 Инструкция по обновлению бота на сервере

## Обновления в этой версии:

1. ✅ Узкий фокус: ТОЛЬКО Facebook/Instagram таргет + Performance marketing
2. ✅ Компактный формат уведомлений (без текста сообщения)
3. ✅ Новые ключевые слова специфичные для FB/IG

---

## 📦 Шаг 1: Загрузите изменения на сервер

### Вариант A: Через Git (рекомендуется)

```bash
# На локальной машине
cd /Users/kirill/Dev\ Dev\ Dev/telegram_rassylshik
git add .
git commit -m "Narrow focus: FB/IG targeting + performance only"
git push origin main

# На сервере
cd ~/telegram_rassylshik
git pull origin main
```

### Вариант B: Через прямое копирование файлов

Скопируйте эти файлы на сервер:
- `bot.py`
- `message_processor.py`

```bash
# С локальной машины
scp bot.py root@YOUR_SERVER_IP:~/telegram_rassylshik/
scp message_processor.py root@YOUR_SERVER_IP:~/telegram_rassylshik/
```

---

## 🔄 Шаг 2: Перезапустите бота

```bash
# Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# Перейдите в директорию проекта
cd ~/telegram_rassylshik

# Пересоберите и перезапустите контейнер
docker compose down
docker compose up --build -d

# Проверьте логи
docker compose logs -f bot
```

---

## ✅ Шаг 3: Проверка работы

Проверьте последние логи:
```bash
docker compose logs --tail=50 bot
```

Должны видеть:
```
✓ Бот авторизован как: ...
✓ Загружено XX каналов для мониторинга
✓ Бот успешно запущен и готов к работе!
```

---

## 🎯 Что изменилось в фильтрации:

### ❌ БОЛЬШЕ НЕ ПРОЙДУТ:
- Вакансии про VK, TikTok, YouTube
- SMM без упоминания FB/IG
- SEO, контент-маркетинг
- Email-маркетинг
- Яндекс.Директ, MyTarget

### ✅ ТЕПЕРЬ ПРОХОДЯТ ТОЛЬКО:
- Facebook/Instagram таргетолог
- Performance маркетолог (FB/IG)
- Meta Ads специалист
- Любые вакансии где есть И Facebook/Instagram И таргет/performance

---

## 📊 Новый формат уведомлений:

**До:**
```
🎯 Новая вакансия!

💰 Оплата: 80000 ₽ (в месяц)

📍 Чат: ...
🛠 Навыки: ...

🔗 Перейти: ...

Контакты:
   ✉️ @username

────────────────────────────────────────
[800 символов текста вакансии]
────────────────────────────────────────
```

**После:**
```
🎯 Новая вакансия!

📍 Чат: ...
🛠 Навыки: ...

🔗 Перейти: ...

Контакты:
   ✉️ @username
```

Компактнее, без лишней информации!

---

## 🆘 Если что-то не работает:

### Проблема: Бот не запускается
```bash
docker compose logs bot
```

### Проблема: Не приходят уведомления
Проверьте что в `.env` правильно указан:
```
NOTIFICATION_CHANNEL_ID=-1003448722182
```

### Проблема: Приходит слишком много / мало уведомлений
Проверьте файл `channels.txt` - в нем должны быть каналы связанные с таргетом/маркетингом.

---

## 📝 Дополнительно

Если хотите вернуться к старой версии:
```bash
git log --oneline  # Посмотреть историю коммитов
git checkout <commit-hash>  # Откатиться на нужный коммит
```

Если нужна помощь:
1. Проверьте логи: `docker compose logs bot`
2. Проверьте статус: `docker compose ps`
3. Перезапустите: `docker compose restart bot`

