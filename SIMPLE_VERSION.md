# Упрощенная версия (без AI) 🚀

Эта версия бота работает **БЕЗ Ollama AI** - только с простой фильтрацией по ключевым словам и маркерам вакансий.

## Преимущества простой версии

✅ **Быстрая работа** - нет задержек на AI анализ  
✅ **Меньше ресурсов** - не нужно 4GB RAM для Ollama  
✅ **Проще настройка** - один контейнер вместо двух  
✅ **Надежнее** - нет зависимости от AI сервиса  

## Как работает фильтрация

Бот считает сообщение релевантным если находит:

### 1. Технические ключевые слова
- Языки: python, javascript, java, c++, go, rust, php...
- Фреймворки: django, flask, react, vue, angular...
- Базы данных: postgresql, mysql, mongodb, redis...
- DevOps: docker, kubernetes, aws, azure...
- И многое другое (смотрите `message_processor.py`)

### 2. Маркеры вакансий
- Русские: вакансия, ищем, требуется, нужен, работа, удаленно, фриланс, зарплата...
- Английские: vacancy, hiring, looking for, remote, freelance, salary...
- Символы: ₽, $, от, до

## Быстрый старт

```bash
# 1. Настройте .env (без Ollama настроек!)
cp .env.example .env
nano .env

# Заполните только:
API_ID=your_api_id
API_HASH=your_api_hash
PHONE=+1234567890
NOTIFICATION_USER_ID=123456789

# 2. Добавьте чаты
nano channels.txt

# 3. Запустите (НАМНОГО быстрее!)
./start.sh

# Или вручную:
docker-compose up -d
```

## Минимальные требования

- ✅ Всего **512MB RAM** (вместо 4GB!)
- ✅ Docker
- ✅ 1GB диск

## Пример уведомления

```
🎯 Новое сообщение о работе!

📍 Чат: Python Jobs Russia
🛠 Найденные навыки: python, django, postgresql, docker

🔗 Ссылка: https://t.me/pythonrujobs/12345

👤 Контакт автора:
   @recruiter_ivan
   Иван Петров

📄 Текст сообщения:
────────────────────────────────────────
Требуется Python разработчик...
(превью текста)
────────────────────────────────────────

💬 Предложенный ответ:
────────────────────────────────────────
Здравствуйте! Меня заинтересовала ваша вакансия...
────────────────────────────────────────
```

## Настройка фильтрации

### Добавить свои ключевые слова

Отредактируйте `message_processor.py`, найдите `keywords_db` (~строка 74):

```python
keywords_db = [
    # Добавьте свои технологии
    'python', 'javascript', ...,
    'ваша_технология',
]
```

### Добавить свои маркеры вакансий

В `bot.py`, найдите `job_markers` (~строка 186):

```python
job_markers = [
    'вакансия', 'vacancy', ...,
    'ваш_маркер',
]
```

## Переключение обратно на AI версию

Если захотите вернуть Ollama:

1. Раскомментируйте Ollama в `docker-compose.yml`
2. Восстановите импорт в `bot.py`: `from ai_qualifier import ai_qualifier`
3. Замените `process_message()` на версию с AI
4. Запустите Ollama: `docker-compose up -d ollama`

## FAQ

**Q: Будет ли много ложных срабатываний?**  
A: Фильтр настроен консервативно. Если слишком много - уберите общие слова из маркеров.

**Q: А если хочу ВСЕГДА получать сообщения из определенного чата?**  
A: Можно добавить специальную логику в `bot.py` - проверять chat_id и пропускать без фильтрации.

**Q: Можно ли добавить фильтр по НЕжелательным словам?**  
A: Да! Добавьте blacklist проверку в `_check_relevance()`.

## Производительность

Без AI бот обрабатывает сообщения **мгновенно**:
- С AI: ~30 секунд на сообщение
- Без AI: ~0.1 секунда на сообщение

**В 300 раз быстрее!** 🚀

## Команды управления

```bash
# Запуск
docker-compose up -d

# Логи
docker-compose logs -f bot

# Остановка
docker-compose down

# Перезапуск (после изменения кода)
docker-compose restart bot
```

---

**Совет:** Начните с простой версии. Если будет нужна более умная квалификация - всегда можно добавить AI позже!

