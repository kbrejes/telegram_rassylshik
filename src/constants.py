"""
Константы проекта

Централизованное хранение констант для избежания дублирования.
"""

# Префиксы служебных сообщений в топиках CRM
# Используются для фильтрации сообщений от бота/AI/агентов
SERVICE_MESSAGE_PREFIXES = (
    "🤖 **Агент (",
    "🤖 **Агент:**",
    "🤖 **AI:",
    "📌 **Новый контакт:",
    "📋 **Вакансия из",
    "👤 **",
)


def is_service_message(text: str) -> bool:
    """
    Проверяет, является ли сообщение служебным.

    Args:
        text: Текст сообщения

    Returns:
        True если сообщение служебное (от бота/AI/агентов)
    """
    if not text:
        return False
    return any(text.startswith(prefix) for prefix in SERVICE_MESSAGE_PREFIXES)
